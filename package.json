{
  "name": "hack-ens-recovery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "rescue": "bun run rescue.ts",
    "simulate": "bun run simulate.ts",
    "run-live": "bun run run-live.ts"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7",
    "@onchain-ai/agent-sdk": "github:trustless-ai/agent-sdk",
    "ethers": "^6.0.0"
  }
}
